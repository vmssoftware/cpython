# Requires repository and build on VILE
# --no-index
# $ def PYTH$WHL_BUILT WORK7:[VORFOLOMEEV.PYTH$WHL_BUILT.]
# python -m pip wheel -w /PYTH$WHL_BUILT -f /PYTH$WHL_BUILT -f /pyth$whl -f /pyth$rep --no-cache-dir --no-index --no-deps <NAME>
Brotli
# requires 64-bit libraries, valid oss$root (define OSS$ROOT WORK7:[VORFOLOMEEV.OSS.X86.])
Pillow
awscli
# $ define libffi$root? (define libffi$root WORK7:[VORFOLOMEEV.LIBFFI.])
cffi
# $ define CRYPTOGRAPHY_DONT_BUILD_RUST "1"
cryptography
cython
httpbin
perfdat
pytest
reportlab
numpy
# not availible for x86
# psycopg2
# not availible for x86 yet (long doubles)
pandas
pyrsistent
pywebui
# get the code from github, create tar and copy it to repository (use branch tag 5.4.1)
PyYAML
ruamel.yaml.clib
# copy PYTH$WHL_BUILT:[000000]*.* PYTH$WHL:[000000] /log
